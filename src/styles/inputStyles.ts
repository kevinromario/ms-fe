import { textBase } from "./typography";

export const inputBase = `${textBase} w-full rounded-md border border-gray-300 px-4 py-2 placeholder-gray-400 focus:outline-none focus:ring-1 focus:ring-gray-300 placeholder:underline placeholder:underline-offset-3`;

export const textareaBase = "resize-none " + inputBase;
